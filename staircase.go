package main

import (
    "fmt"
)

func main() {
    var N int
    fmt.Scanf("%d", &N)
    multi := make([][]string, 0, N)
    for i :=0; i < N; i++ {
        for j := i; j < N, j++{
            fmt.Printf(" ")
        }
        fmt.Printf("
    }
}
